package globals

object Constants {
  val DidNotAddToSampler = -1 // signifies that we did not add the document to
                              // the sampler in PfLda
  val NoParent = -1 // indicates that particle does not have a parent
}
